#include <stdio.h>
#include <math.h>

int main() {

	setvbuf(stdout, NULL, _IONBF, 0);

	int k, j;

	for(k = 1, j = 2; k <= 16; k++)
	{
		printf("The %i power of 2 is %i\n", k, (int)pow(j, k));
	}
    return 0;
}
